# Driver/orchestrator environment (NO torch_xla, NO JAX, NO vLLM)
ray>=2.50.0
transformers>=4.30.0
torch==2.8.0+cpu --extra-index-url https://download.pytorch.org/whl/cpu  # CPU-only, no CUDA
tensorboard>=2.20.0
numpy>=1.21.0
pandas>=2.3.0
psutil>=5.8.0
virtualenv>=20.0.0      # for Ray runtime_env.pip venv creation
